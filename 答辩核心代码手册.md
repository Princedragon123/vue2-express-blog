
# 论文答辩核心代码手册 📚
（只记这些就够了！）

---

## 1️⃣ JWT生成（后端）
**文件：** `src/utils/jwt.js`
```javascript
const jwt = require('jsonwebtoken');

exports.generateToken = (userId, role) => {
    return jwt.sign(
        { id: userId, role: role },  // Payload：用户信息
        process.env.JWT_SECRET,       // 密钥
        { expiresIn: '7d' }            // 过期时间
    );
};
```

**答辩话术：**
"生成JWT时，我用jsonwebtoken库的sign方法，把用户ID和角色放在Payload里，用密钥签名，设置7天过期时间。"

---

## 2️⃣ JWT验证（中间件）
**文件：** `src/middlewares/auth.js`
```javascript
exports.authMiddleware = async (req, res, next) => {
    const token = req.header('Authorization')?.replace('Bearer ', '');
    if (!token) return res.status(401).json({ message: '未授权' });
    
    const decoded = jwt.verifyToken(token);  // 验证token
    req.user = await User.findById(decoded.id);  // 查找用户
    next();  // 继续处理
};
```

**答辩话术：**
"认证中间件从请求头获取token，去掉Bearer前缀，然后验证token的签名和过期时间，验证通过后把用户信息挂载到req对象上。"

---

## 3️⃣ 密码加盐（bcrypt）
**文件：** `src/models/User.js`
```javascript
const bcrypt = require('bcryptjs');

userSchema.pre('save', async function() {
    if (!this.isModified('password')) return;
    
    const salt = await bcrypt.genSalt(10);  // 生成盐
    this.password = await bcrypt.hash(this.password, salt);  // 哈希
});

userSchema.methods.comparePassword = async function(candidatePassword) {
    return await bcrypt.compare(candidatePassword, this.password);  // 验证密码
};
```

**答辩话术：**
"密码加密用bcryptjs，在保存用户前自动加盐哈希，验证密码时用compare方法比较明文和哈希值。"

---

## 4️⃣ 前端请求拦截器（自动加token）
**文件：** `src/vue/utils/api.js`
```javascript
api.interceptors.request.use((config) => {
    const token = auth.getToken();
    if (token) {
        config.headers.Authorization = `Bearer ${token}`;  // 自动加token
    }
    return config;
});
```

**答辩话术：**
"用Axios的请求拦截器，每次发送请求前自动从auth工具获取token，加到Authorization请求头里。"

---

## 5️⃣ 前端路由守卫
**文件：** `src/vue/router/index.js`
```javascript
router.beforeEach((to, from, next) => {
    const publicRoutes = ['/login', '/register'];
    const isPublicRoute = publicRoutes.some(path => to.path.startsWith(path));
    
    if (!isPublicRoute && !auth.isLoggedIn()) {
        next('/login');  // 未登录跳登录页
    } else {
        next();  // 继续
    }
});
```

**答辩话术：**
"路由守卫在每次跳转前检查，公开路由（登录、注册）直接放行，其他路由需要登录，未登录就跳转到登录页。"

---

## 6️⃣ Yup数据验证
**文件：** `src/utils/validation.js`
```javascript
const yup = require('yup');

exports.registerValidation = yup.object().shape({
    username: yup.string().required().min(3).max(20),
    email: yup.string().required().email(),
    password: yup.string().required().min(6)
});
```

**答辩话术：**
"用Yup库做数据验证，链式调用定义验证规则，比如用户名必填、3-20个字符，邮箱必填且格式正确等。"

---

## 🎯 答辩时的核心原则

1. **只说你理解的**，不懂就说"这个部分我主要参考了文档，核心逻辑我清楚"
2. **用流程图代替代码**，画图讲流程比背代码更有效
3. **强调你学到了什么**，而不是你写了多少代码
4. **诚实**，老师能看出来你是不是真的理解

---

## 📝 答辩高频问题及标准答案

### Q: JWT的三个部分是什么？
A: Header（头部）、Payload（载荷）、Signature（签名）

### Q: 为什么要用bcrypt加盐？
A: 防止彩虹表攻击，即使两个用户密码相同，哈希值也不同。

### Q: 路由守卫的作用是什么？
A: 在页面前检查登录状态，未登录用户不能访问需要认证的页面。

### Q: 为什么要用请求拦截器？
A: 统一给所有API请求加token，不用每个请求都手动写。

---

## 💡 答辩小技巧

- ✅ 带上笔记本，上面画好流程图，答辩时可以指着图讲
- ✅ 不要背代码，要理解代码的作用
- ✅ 老师问到时，可以说"让我想一下，这个功能的实现思路是..."
- ✅ 遇到不会的问题，不要慌，可以说"这个问题我现在记不太清楚了，但我知道怎么查文档解决"

祝答辩顺利！🎉
