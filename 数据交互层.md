# 数据交互层设计和实现

## 1. 设计方案
### 1.1 架构设计
数据交互层采用分层架构设计，主要包括以下几个核心组件：

1. **HTTP 客户端层**：基于 Axios 实现，负责处理 HTTP 请求和响应
2. **API 方法层**：封装所有 API 调用方法，提供统一的接口
3. **拦截器层**：处理请求和响应的拦截，实现认证、错误处理等
4. **实时通信层**：基于 WebSocket 实现实时消息和通知
5. **认证状态管理层**：管理用户认证状态和 Token

**架构图**：
```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  前端应用层     │     │  API 方法层     │     │  HTTP 客户端层  │
└─────────────────┘────>└─────────────────┘────>└─────────────────┘
          ^                           │                           │
          │                           v                           v
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  认证状态管理   │<────┘  拦截器层       │<────┘  实时通信层     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                                              │
                                                              v
                                                    ┌─────────────────┐
                                                    │  后端服务       │
                                                    └─────────────────┘
```

### 1.2 核心设计原则
1. **统一 API 调用接口**：所有 API 调用通过封装的方法进行，确保一致性
2. **集中式错误处理**：通过响应拦截器统一处理错误，减少重复代码
3. **自动 Token 管理**：实现 Token 自动刷新机制，提高用户体验
4. **模块化设计**：按功能模块组织 API 方法，提高代码可维护性
5. **类型安全**：明确 API 请求和响应的数据结构
6. **可扩展性**：支持添加新的 API 模块和功能

### 1.3 技术选型
| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| Axios | 1.13.5 | HTTP 客户端 | 支持 Promise、拦截器、取消请求等特性，社区活跃 |
| WebSocket | 内置 | 实时通信 | 提供全双工通信，适合实时消息和通知 |
| JWT | - | 认证 | 无状态认证，便于水平扩展 |
| Promise | ES6+ | 异步处理 | 统一异步代码风格，提高可读性 |

## 2. 实现细节
### 2.1 Axios 实例配置
```javascript
// src/vue/utils/api.js
import axios from 'axios';

// 创建axios实例
const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
});
```
- **baseURL**：设置 API 基础路径，使用相对路径便于部署
- **timeout**：设置请求超时时间为 10 秒，避免请求长时间无响应
- **headers**：设置默认请求头为 JSON 格式

### 2.2 请求拦截器
```javascript
// 请求拦截器
api.interceptors.request.use(
  (config) => {
    // 从认证状态管理获取token
    const token = auth.getToken();
    if (token) {
      // 添加Authorization头
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    // 处理请求错误
    console.error('API请求错误:', error);
    return Promise.reject(error);
  }
);
```
- **Token 注入**：自动将 Token 添加到请求头，确保 API 调用的安全性
- **错误处理**：捕获并记录请求错误

### 2.3 响应拦截器
```javascript
// 响应拦截器
api.interceptors.response.use(
  (response) => {
    // 处理响应数据
    const data = response.data;
    if (data.success) {
      return data;
    } else {
      // 处理业务错误
      return Promise.reject(new Error(data.message || '请求失败'));
    }
  },
  async (error) => {
    // 处理响应错误
    console.error('API响应错误:', error);
    
    // 处理常见错误
    if (error.response) {
      switch (error.response.status) {
        case 401:
          // 未授权，尝试刷新token
          // ... token刷新逻辑
          break;
        case 403:
          // 禁止访问
          alert('权限不足，无法访问该资源');
          break;
        case 404:
          // 资源不存在
          alert('请求的资源不存在');
          break;
        case 500:
          // 服务器错误
          alert('服务器内部错误，请稍后重试');
          break;
        default:
          // 其他错误
          alert(`请求失败: ${error.response.data.message || '未知错误'}`);
      }
    } else if (error.request) {
      // 请求已发出但没有收到响应
      alert('网络错误，无法连接到服务器');
    } else {
      // 请求配置错误
      alert('请求配置错误');
    }
    
    return Promise.reject(error);
  }
);
```
- **响应数据处理**：统一处理响应数据格式，只返回成功的数据
- **错误处理**：根据 HTTP 状态码处理不同类型的错误
- **Token 刷新**：实现自动 Token 刷新机制，提高用户体验
- **网络错误处理**：处理网络连接问题和请求配置错误

### 2.4 API 方法封装
```javascript
// API方法封装
const apiMethods = {
  // 认证相关API
  auth: {
    login: (data) => api.post('/auth/login', data),
    register: (data) => api.post('/auth/register', data),
    getCurrentUser: () => api.get('/auth/me'),
    logout: () => api.post('/auth/logout')
  },
  
  // 博客相关API
  blogs: {
    getList: (params) => api.get('/blogs', { params }),
    getDetail: (id) => api.get(`/blogs/${id}`),
    create: (data) => api.post('/blogs', data),
    update: (id, data) => api.put(`/blogs/${id}`, data),
    delete: (id) => api.delete(`/blogs/${id}`),
    // ... 其他博客相关API
  },
  
  // 消息相关API
  messages: {
    getContacts: (params) => api.get('/messages/contacts', { params }),
    getHistory: (userId, params) => api.get(`/messages/${userId}`, { params }),
    send: (data) => api.post('/messages', data),
    // ... 其他消息相关API
  },
  
  // 通知相关API
  notifications: {
    getList: (params) => api.get('/notifications', { params }),
    // ... 其他通知相关API
  },
  
  // 用户相关API
  users: {
    getInfo: (id) => api.get(`/users/${id}`),
    update: (data) => api.put('/users/profile', data),
    follow: (id) => api.post(`/users/${id}/follow`),
    unfollow: (id) => api.delete(`/users/${id}/follow`),
    // ... 其他用户相关API
  }
};
```
- **模块化组织**：按功能模块组织 API 方法，提高代码可读性
- **参数处理**：支持传递查询参数和请求体
- **RESTful 风格**：遵循 RESTful API 设计规范
- **类型安全**：明确 API 方法的参数和返回值

### 2.5 实时通信实现
```javascript
// src/server.js
// 初始化 WebSocket 服务
socketService.init(server);

// socket.js（WebSocket事件处理）
const clients = new Map();

// 处理新连接
wss.on('connection', (ws) => {
  // 处理消息
  ws.on('message', async (message) => {
    // 解析消息
    const parsedMessage = JSON.parse(message);
    // 根据消息类型处理
    switch (parsedMessage.type) {
      case 'init': // 初始化用户连接
      case 'private_message': // 私聊消息
      case 'send_friend_request': // 好友请求
      case 'respond_friend_request': // 响应好友请求
    }
  });
});
```
- **WebSocket 服务器**：基于 Node.js 内置的 WebSocket 模块
- **连接管理**：使用 Map 存储用户 ID 和 WebSocket 连接的映射
- **消息处理**：根据消息类型处理不同的业务逻辑
- **实时通信**：支持私聊消息、好友请求等实时功能

## 3. 接口设计
### 3.1 认证模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/auth/register` | POST | 用户注册 | 否 |
| `/api/auth/login` | POST | 用户登录 | 否 |
| `/api/auth/me` | GET | 获取当前用户信息 | 是 |
| `/api/auth/logout` | POST | 用户登出 | 是 |
| `/api/auth/refresh-token` | POST | 刷新Token | 否 |

### 3.2 用户模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/users/:userId/follow` | POST | 关注用户 | 是 |
| `/api/users/:userId/follow` | DELETE | 取消关注用户 | 是 |
| `/api/users/:userId/following` | GET | 获取用户关注列表 | 否 |
| `/api/users/:userId/followers` | GET | 获取用户粉丝列表 | 否 |
| `/api/users/search` | GET | 搜索用户 | 否 |
| `/api/users/check-follow/:userId` | GET | 检查是否关注用户 | 是 |
| `/api/users/upload-avatar` | POST | 上传头像 | 是 |
| `/api/users/upload-cover` | POST | 上传背景图 | 是 |
| `/api/users/update-privacy-settings` | PUT | 更新隐私设置 | 是 |
| `/api/users/update-notification-settings` | PUT | 更新通知设置 | 是 |
| `/api/users/:userId` | GET | 获取用户信息 | 否（可选认证） |
| `/api/users/:userId` | PUT | 更新用户信息 | 是 |
| `/api/users/:userId/liked-posts` | GET | 获取用户点赞的文章 | 否 |
| `/api/users/:userId/bookmarked-posts` | GET | 获取用户收藏的文章 | 否 |
| `/api/users/update-profile` | PUT | 修改用户名 | 是 |
| `/api/users/change-password` | PUT | 修改密码 | 是 |

### 3.3 博客模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/blogs` | GET | 获取博客列表 | 否 |
| `/api/blogs` | POST | 创建新文章 | 是 |
| `/api/blogs/hot` | GET | 获取热门博客 | 否 |
| `/api/blogs/latest` | GET | 获取最新博客 | 否 |
| `/api/blogs/bookmarks` | GET | 获取用户收藏列表 | 是 |
| `/api/blogs/my` | GET | 获取我的文章列表 | 是 |
| `/api/blogs/drafts` | GET | 获取我的草稿列表 | 是 |
| `/api/blogs/search` | GET | 搜索文章 | 否 |
| `/api/blogs/hot-searches` | GET | 获取热门搜索词条 | 否 |
| `/api/blogs/:id` | GET | 获取博客详情 | 否 |
| `/api/blogs/:blogId/comments` | GET | 获取博客评论列表 | 否 |
| `/api/blogs/:blogId/comments` | POST | 创建评论 | 是 |
| `/api/blogs/comments/:commentId/like` | POST | 点赞评论 | 是 |
| `/api/blogs/comments/:commentId` | DELETE | 删除评论 | 是 |
| `/api/blogs/:id/like` | POST | 点赞博客 | 是 |
| `/api/blogs/:id/like` | DELETE | 取消点赞博客 | 是 |
| `/api/blogs/:id/is-liked` | GET | 检查是否已点赞 | 是 |
| `/api/blogs/:id/is-bookmarked` | GET | 检查是否已收藏 | 是 |
| `/api/blogs/:id/bookmark` | POST | 收藏博客 | 是 |
| `/api/blogs/:id/bookmark` | DELETE | 取消收藏博客 | 是 |
| `/api/blogs/:id` | PUT | 更新文章 | 是 |
| `/api/blogs/:id` | DELETE | 删除文章 | 是 |

### 3.4 话题模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/topics` | POST | 创建话题 | 是 |
| `/api/topics` | GET | 获取话题列表 | 否 |
| `/api/topics/:id` | GET | 获取话题详情 | 否 |
| `/api/topics/:id/follow` | POST | 关注话题 | 是 |
| `/api/topics/:id/follow` | DELETE | 取消关注话题 | 是 |
| `/api/topics/:id/articles` | GET | 获取话题下的文章 | 否 |
| `/api/topics/search` | GET | 搜索话题 | 否 |

### 3.5 消息模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/messages/contacts` | GET | 获取联系人列表 | 是 |
| `/api/messages/:userId` | GET | 获取与特定用户的消息历史 | 是 |
| `/api/messages` | POST | 发送新消息 | 是 |
| `/api/messages/upload` | POST | 上传消息附件 | 是 |
| `/api/messages/read` | PUT | 标记消息为已读 | 是 |
| `/api/messages/:id` | DELETE | 删除消息 | 是 |
| `/api/messages/share` | POST | 分享博客 | 是 |

### 3.6 通知模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/notifications` | POST | 创建通知 | 是 |
| `/api/notifications` | GET | 获取用户通知列表 | 是 |
| `/api/notifications/:notificationId/read` | PUT | 标记通知为已读 | 是 |
| `/api/notifications/read-all` | PUT | 标记所有通知为已读 | 是 |
| `/api/notifications/:notificationId` | DELETE | 删除通知 | 是 |

### 3.7 管理员模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/admin/stats` | GET | 获取系统统计数据 | 是（管理员） |
| `/api/admin/blogs` | GET | 获取所有博客（支持筛选、搜索） | 是（管理员） |
| `/api/admin/blogs/:blogId` | DELETE | 删除博客 | 是（管理员） |
| `/api/admin/blogs/:blogId/status` | PUT | 更新博客状态 | 是（管理员） |
| `/api/admin/users` | GET | 获取用户列表 | 是（管理员） |
| `/api/admin/users/:userId` | GET | 查看用户详情 | 是（管理员） |
| `/api/admin/users/:userId` | PUT | 更新用户信息 | 是（管理员） |
| `/api/admin/users/:userId/ban` | PUT | 封禁用户 | 是（管理员） |
| `/api/admin/users/:userId/unban` | PUT | 解封用户 | 是（管理员） |
| `/api/admin/users/:userId` | DELETE | 删除用户 | 是（管理员） |
| `/api/admin/categories` | GET | 获取分类列表 | 是（管理员） |

### 3.8 浏览记录模块
| URL | 方法 | 功能 | 认证要求 |
|-----|------|------|----------|
| `/api/history` | POST | 添加浏览记录 | 是 |
| `/api/history` | GET | 获取浏览记录 | 是 |
| `/api/history` | DELETE | 清空浏览记录 | 是 |
| `/api/history/:id` | DELETE | 删除浏览记录 | 是 |

## 4. 接口说明
### 4.1 认证要求
- **否**：无需认证即可访问
- **是**：需要在请求头中携带有效的JWT令牌
- **是（管理员）**：需要在请求头中携带有效的JWT令牌，且用户角色为admin

### 4.2 响应格式
所有接口的响应格式统一为JSON格式，包含以下字段：
- `success`：布尔值，表示操作是否成功
- `message`：字符串，操作结果的描述信息
- `data`：对象或数组，返回的数据
- `pagination`：对象，分页信息（仅适用于列表接口）

### 4.3 错误处理
接口错误处理遵循以下原则：
- **400 Bad Request**：请求参数错误
- **401 Unauthorized**：未认证或认证失败
- **403 Forbidden**：权限不足
- **404 Not Found**：资源不存在
- **500 Internal Server Error**：服务器内部错误

### 4.4 分页参数
对于支持分页的接口，可使用以下参数：
- `page`：整数，页码，默认1
- `limit`：整数，每页数量，默认10

### 4.5 搜索参数
对于支持搜索的接口，可使用以下参数：
- `search`：字符串，搜索关键词

## 5. 接口使用示例
### 5.1 认证示例
```javascript
// 登录
const loginResponse = await apiMethods.auth.login({
  email: 'user@example.com',
  password: 'password123'
});

// 获取当前用户信息
const userInfo = await apiMethods.auth.getCurrentUser();
```

### 5.2 博客示例
```javascript
// 获取博客列表
const blogsResponse = await apiMethods.blogs.getList({
  page: 1,
  limit: 10
});

// 创建新文章
const createResponse = await apiMethods.blogs.create({
  title: '示例文章',
  content: '文章内容',
  status: 'published'
});
```

### 5.3 用户示例
```javascript
// 关注用户
await apiMethods.users.follow('60d7f1e8c6a1b23a4c5d6e7e');

// 获取用户信息
const userInfo = await apiMethods.users.getInfo('60d7f1e8c6a1b23a4c5d6e7e');
```

## 6. 最佳实践
### 6.1 API 调用最佳实践
1. **错误处理**：使用 try-catch 捕获 API 调用错误
2. **参数验证**：在调用 API 前验证参数的合法性
3. **请求取消**：对于可能被用户中断的请求，使用 Axios 的取消功能
4. **批量请求**：对于多个相关请求，使用 Promise.all 并行执行

### 6.2 性能优化
1. **请求合并**：合并多个相关的 API 请求
2. **缓存策略**：缓存频繁访问的数据
3. **延迟加载**：对于非关键数据，使用延迟加载
4. **网络优化**：使用 HTTP/2 或 HTTP/3，启用压缩

### 6.3 安全性
1. **Token 管理**：安全存储和管理 JWT 令牌
2. **HTTPS**：使用 HTTPS 保护数据传输
3. **输入验证**：在前端和后端都进行输入验证
4. **防止 CSRF**：实现 CSRF 保护

## 7. 总结
数据交互层是连接前端和后端的桥梁，其设计和实现直接影响系统的性能、安全性和用户体验。本项目采用了现代化的设计方案，包括：

1. **基于 Axios 的 HTTP 客户端**：提供了强大的请求处理能力，支持拦截器、取消请求等特性
2. **模块化的 API 设计**：按功能模块组织 API 方法，提高代码可维护性
3. **完善的错误处理机制**：统一处理各种错误场景，提供友好的用户提示
4. **Token 自动管理**：实现 Token 自动刷新，提高用户体验
5. **WebSocket 实时通信**：支持实时消息和通知功能

通过这些设计和实现，数据交互层能够高效、安全地处理前端与后端之间的通信，为整个系统提供可靠的数据传输保障。同时，通过持续的优化和改进，可以进一步提高系统的性能和可靠性。

数据交互层的设计遵循了以下核心原则：

- **统一性**：统一 API 调用接口和错误处理
- **可靠性**：确保数据传输的可靠性和安全性
- **可扩展性**：支持添加新的功能和模块
- **性能优化**：提高请求和响应的效率
- **用户体验**：提供流畅的交互体验

这些原则确保了数据交互层能够满足系统的需求，同时为未来的扩展和维护打下了坚实的基础。
