# 点赞功能修复方案

## 问题分析

### 1. 数据不准确
- **原因**：虽然系统已经创建了 `Like` 模型来记录用户和博客之间的点赞关系，但 `BlogController.js` 中的 `likeBlog` 和 `unlikeBlog` 方法没有使用这个模型，导致一个用户可以对同一篇博客多次点赞。
- **影响**：测试数据中出现了一个用户对同一篇博客点9个赞的情况，导致点赞数虚高。

### 2. 前端功能不完整
- **原因**：前端的 `Detail.vue` 组件只实现了点赞功能，没有实现取消点赞功能，也没有检查用户是否已经点赞过博客。
- **影响**：用户无法知道自己是否已经点赞过某篇博客，也无法取消点赞。

## 解决方案

### 1. 修改后端点赞逻辑

修改 `BlogController.js` 中的 `likeBlog` 和 `unlikeBlog` 方法，使用 `Like` 模型来确保一个用户只能对一篇博客点一次赞：

```javascript
// 点赞博客
likeBlog: async (req, res) => {
    try {
        const { id } = req.params;
        const userId = req.user.id;
        
        // 检查是否已经点赞过
        const existingLike = await Like.findOne({ 
            user: userId, 
            contentId: id, 
            contentType: 'blog' 
        });
        if (existingLike) {
            return res.status(400).json({ success: false, message: '已经点赞过该博客' });
        }
        
        // 更新博客的点赞数
        const blog = await Blog.findByIdAndUpdate(
            id,
            { $inc: { likes: 1 } },
            { new: true }
        );
        
        if (!blog) {
            return res.status(404).json({ success: false, message: '博客不存在' });
        }
        
        // 更新博客作者的获赞数
        await User.findByIdAndUpdate(blog.author, { $inc: { 'stats.likesCount': 1 } });
        
        // 创建点赞记录
        const like = new Like({
            user: userId,
            contentId: id,
            contentType: 'blog'
        });
        await like.save();
        
        // 创建点赞通知
        if (blog.author.toString() !== userId.toString()) {
            const notification = new Notification({
                receiver: blog.author.toString(),
                sender: userId,
                type: 'like',
                content: `点赞了你的博客《${blog.title}》`,
                resourceId: id,
                resourceType: 'blog'
            });
            await notification.save();
        }
        
        res.json({ success: true, data: { likes: blog.likes } });
    } catch (error) {
        console.error('点赞博客失败:', error);
        res.status(500).json({ success: false, message: '点赞博客失败' });
    }
},

// 取消点赞博客
unlikeBlog: async (req, res) => {
    try {
        const { id } = req.params;
        const userId = req.user.id;
        
        // 检查是否已经点赞过
        const like = await Like.findOneAndDelete({ 
            user: userId, 
            contentId: id, 
            contentType: 'blog' 
        });
        if (!like) {
            return res.status(404).json({ success: false, message: '未点赞过该博客' });
        }
        
        // 更新博客的点赞数
        const blog = await Blog.findByIdAndUpdate(
            id,
            { $inc: { likes: -1 } },
            { new: true }
        );
        
        if (!blog) {
            return res.status(404).json({ success: false, message: '博客不存在' });
        }
        
        // 确保点赞数不为负数
        if (blog.likes < 0) {
            blog.likes = 0;
            await blog.save();
        }
        
        // 更新博客作者的获赞数
        await User.findByIdAndUpdate(blog.author, { $inc: { 'stats.likesCount': -1 } });
        
        res.json({ success: true, data: { likes: blog.likes } });
    } catch (error) {
        console.error('取消点赞博客失败:', error);
        res.status(500).json({ success: false, message: '取消点赞博客失败' });
    }
}
```

### 2. 修改前端点赞逻辑

修改 `Detail.vue` 组件，添加检查用户是否已经点赞过博客的逻辑，并实现取消点赞的功能：

```javascript
// 检查用户是否已经点赞过博客
async checkLikeStatus() {
  try {
    // 这里需要实现一个 API 来检查用户是否已经点赞过博客
    // 或者在获取博客详情时，同时返回用户的点赞状态
    // 暂时跳过，后续实现
  } catch (error) {
    console.error('检查点赞状态失败:', error);
  }
},

// 点赞博客
async likeBlog() {
  try {
    if (this.isLiked) {
      // 已经点赞过，执行取消点赞
      const result = await api.blogs.unlike(this.blogId);
      if (result.success) {
        // 更新博客点赞数
        this.blogData.likes -= 1;
        this.isLiked = false;
      }
    } else {
      // 未点赞过，执行点赞
      const result = await api.blogs.like(this.blogId);
      if (result.success) {
        // 更新博客点赞数
        this.blogData.likes += 1;
        this.isLiked = true;
      }
    }
  } catch (error) {
    console.error('点赞博客失败:', error);
    alert('操作失败，请重试');
  }
}
```

### 3. 清理测试数据

创建一个脚本文件来清理测试数据，确保点赞数的准确性：

```javascript
// cleanTestData.js
const mongoose = require('mongoose');
const Blog = require('./src/models/Blog');
const Like = require('./src/models/Like');
const User = require('./src/models/User');

// 连接数据库
mongoose.connect('mongodb://localhost:27017/bloglogin', {
  useNewUrlParser: true,
  useUnifiedTopology: true
});

// 清理测试数据
async function cleanTestData() {
  try {
    // 1. 清空所有点赞记录
    await Like.deleteMany({});
    console.log('点赞记录已清空');
    
    // 2. 重置所有博客的点赞数为 0
    await Blog.updateMany({}, { $set: { likes: 0 } });
    console.log('博客点赞数已重置为 0');
    
    // 3. 重置所有用户的获赞数为 0
    await User.updateMany({}, { $set: { 'stats.likesCount': 0 } });
    console.log('用户获赞数已重置为 0');
    
    console.log('测试数据清理完成');
    process.exit(0);
  } catch (error) {
    console.error('清理测试数据失败:', error);
    process.exit(1);
  }
}

cleanTestData();
```

## 建议

1. **实现点赞状态检查**：在获取博客详情时，同时返回用户的点赞状态，这样前端可以知道用户是否已经点赞过博客。

2. **优化前端用户体验**：根据用户的点赞状态，动态改变点赞按钮的样式和文本，例如点赞后按钮变为红色，显示"已点赞"。

3. **定期清理测试数据**：在开发和测试过程中，定期清理测试数据，确保数据的准确性。

4. **添加数据验证**：在后端添加数据验证，确保点赞数、评论数等统计数据的准确性，防止恶意操作。

5. **实现点赞状态 API**：添加一个 API 端点，用于检查用户是否已经点赞过某篇博客，这样前端可以在页面加载时检查点赞状态。

6. **优化数据库查询**：在查询博客列表时，可以使用聚合查询来同时获取博客的点赞数和用户的点赞状态，减少 API 调用次数。

7. **添加缓存**：对于热门博客的点赞数，可以使用缓存来减少数据库查询次数，提高性能。

8. **实现批量操作**：对于用户的批量点赞操作，可以实现一个批量 API 端点，减少 HTTP 请求次数。

## 总结

通过以上解决方案，博客系统的点赞功能将更加准确和公平，用户体验也会得到提升。同时，这些修改也为系统的后续扩展和优化奠定了基础。