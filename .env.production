# ============================================================
# 生产环境配置文件（宝塔面板部署专用）
# ============================================================
# 【使用说明】
# 1. 复制此文件为 .env：cp .env.production .env
# 2. 根据实际情况修改下面的配置
# 3. 确保此文件不被提交到 Git（已在 .gitignore 中）
# ============================================================

# ------------------------------------------------------------
# MongoDB 数据库配置
# ------------------------------------------------------------
# 本地开发
MONGODB_URI=mongodb://127.0.0.1:27017/bloglogin

# 云服务器 MongoDB（取消注释使用）
# MONGODB_URI=mongodb://用户名:密码@你的IP地址:27017/bloglogin

# MongoDB Atlas 云数据库（取消注释使用）
# MONGODB_URI=mongodb+srv://用户名:密码@集群.mongodb.net/bloglogin?retryWrites=true&w=majority

# ------------------------------------------------------------
# JWT 认证配置
# ------------------------------------------------------------
# JWT 密钥（必须修改！生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"）
JWT_SECRET=请修改为随机生成的密钥_至少32位

# JWT 过期时间
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------
# RSA 密钥配置
# ------------------------------------------------------------
RSA_KEY_SIZE=2048

# ------------------------------------------------------------
# 服务器端口配置
# ------------------------------------------------------------
# 后端服务端口（宝塔面板建议使用 3000-9999 范围内的端口）
PORT=3001

# 运行环境（生产环境必须设为 production）
NODE_ENV=production

# ------------------------------------------------------------
# CORS 跨域配置
# ------------------------------------------------------------
# 允许的前端来源（多个用逗号分隔，* 表示允许所有）
ALLOWED_ORIGINS=*

# 如果前端和后端同源部署，用 * 即可
# 如果分开部署，填写具体域名：
# ALLOWED_ORIGINS=https://yourdomain.com,http://localhost:8080

# ------------------------------------------------------------
# 前端构建配置
# ------------------------------------------------------------
# 前端资源公共路径（一般保持 / 不变）
PUBLIC_PATH=/

# ------------------------------------------------------------
# 文件上传配置
# ------------------------------------------------------------
MAX_FILE_SIZE=10
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# ------------------------------------------------------------
# 会话配置（如果使用 session）
# ------------------------------------------------------------
SESSION_SECRET=请修改为另一个随机密钥

# ------------------------------------------------------------
# 日志配置
# ------------------------------------------------------------
LOG_LEVEL=info
