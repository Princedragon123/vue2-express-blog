# 🐱 博客系统面试准备指南

## 🎯 核心原则：**不说细，只说核！**

面试官不会逐行问代码，重点考察：
1. 项目整体架构理解
2. 核心功能实现思路
3. 技术选型理由
4. 遇到的问题和解决方案

---

## 📚 第一部分：项目整体介绍（必背！）

### 1. 项目概述
- **项目名称**：博客攻略分享平台
- **技术栈**：Vue 2 + Express.js + MongoDB + Webpack
- **项目类型**：前后端分离的单页应用（SPA）
- **核心功能**：博客发布、用户互动、内容管理

### 2. 技术架构
```
┌─────────────────┐
│   前端 (Vue 2)  │
│  - 组件化开发   │
│  - Vue Router   │
│  - Axios HTTP   │
└────────┬────────┘
         │ RESTful API
         ↓
┌─────────────────┐
│  后端 (Express) │
│  - REST API     │
│  - JWT认证      │
│  - 数据库操作   │
└────────┬────────┘
         ↓
┌─────────────────┐
│  数据库 (Mongo) │
│  - 用户表       │
│  - 博客表       │
│  - 互动表       │
└─────────────────┘
```

---

## 💻 第二部分：前端核心知识点（重点掌握！）

### 1. Vue 2 核心概念（你说你懂基础，那就重点说！）

#### ✅ 数据响应式
- **原理**：Object.defineProperty 劫持数据
- **面试回答**："Vue通过数据劫持和发布订阅模式实现响应式，data中的数据变化会自动更新视图"

#### ✅ 组件生命周期
- **重点记4个**：
  - `created`：数据初始化完成，DOM还没渲染
  - `mounted`：DOM渲染完成，可以操作DOM
  - `updated`：数据更新后
  - `beforeDestroy`：组件销毁前

#### ✅ 组件通信
- **父子组件**：props / $emit
- **兄弟组件**：事件总线 / Vuex
- **全局**：Vue.prototype（我们项目用了这个！）

### 2. 项目中的Vue应用（可以直接说！）

#### ✅ Vue.prototype.$http 的使用
**面试官可能问**：为什么这么设计？

**你的回答**：
> "为了统一API调用方式，避免在每个组件都import axios。
> 我们把axios封装好挂载到Vue原型上，所有组件都能通过this.$http调用，
> 这样代码更简洁，也方便统一处理请求和响应。"

#### ✅ 项目中的API封装
看这个文件：`src/vue/utils/api.js`
- **请求拦截器**：自动加Authorization token
- **响应拦截器**：统一处理401、500等错误
- **面试回答**："拦截器可以在请求前自动添加token，响应时统一处理错误，不用每个请求都写重复代码"

### 3. 路由（Vue Router）
- **项目用了**：Vue Router 3.x
- **核心概念**：路由配置、导航守卫、动态路由
- **面试回答**："导航守卫可以检查用户登录状态，没登录就跳转到登录页"

---

## 🎨 第三部分：CSS/样式（不用记细节！）

### 面试官问CSS怎么回答？
> "样式方面我主要负责核心布局，用了Flexbox和Grid布局，
> 响应式设计通过媒体查询实现，整体风格统一。
> 具体的颜色、间距这些细节是参考设计稿调整的。"

**重点掌握这两个布局就够了**：
1. **Flexbox**：一维布局，适合导航、卡片列表
2. **Grid**：二维布局，适合整体页面布局

---

## 🔌 第四部分：核心功能（说你熟悉的！）

### 1. 用户认证模块
- **功能**：登录、注册、JWT token管理
- **技术点**：localStorage/sessionStorage存token
- **面试回答**："登录成功后后端返回JWT token，前端存到localStorage，
  每次请求通过axios拦截器自动加到请求头里"

### 2. 博客列表模块
- **功能**：分页、分类筛选、文章类型筛选
- **技术点**：computed计算属性、watch监听
- **面试回答**："用computed来过滤文章列表，比methods性能更好，
  因为computed有缓存，数据不变不会重新计算"

### 3. 互动功能
- **功能**：点赞、收藏、评论、关注
- **技术点**：异步请求、状态更新
- **面试回答**："互动操作都是异步的，用async/await处理，
  操作成功后立即更新前端状态，提升用户体验"

---

## 🤔 第五部分：常见面试题及答案（直接背！）

### Q1: 说一下你这个项目的技术选型？
**A**: 
> "前端用Vue 2，因为它上手快、文档全、生态好；
> 后端用Express.js，轻量灵活，适合快速开发；
> 数据库用MongoDB，文档型数据库，适合博客这种结构化不强的数据；
> 构建工具用Webpack，模块化打包，支持热更新。"

### Q2: 遇到过什么技术难点？怎么解决的？
**A**: 
> "难点1：API调用方式不统一，有的用fetch有的用axios。
> 解决：封装了统一的axios实例，挂载到Vue.prototype.$http，
> 所有组件统一用this.$http调用，代码更整洁。
> 
> 难点2：token过期处理。
> 解决：在axios响应拦截器里统一处理401错误，
> 自动清除token并跳转到登录页。"

### Q3: 怎么优化项目性能？
**A**: 
> "1. 图片懒加载：用vue-lazyload，图片进入视口才加载
> 2. 路由懒加载：动态import，按需加载组件
> 3.  computed缓存：用computed代替methods做数据过滤
> 4. 防抖节流：搜索输入用防抖，避免频繁请求"

### Q4: 说说Vue的双向绑定原理？
**A**: 
> "Vue 2用Object.defineProperty劫持data的属性，
> 给每个属性加getter和setter。
> 读取数据时触发getter，收集依赖；
> 修改数据时触发setter，通知依赖更新视图。"

---

## 📝 第六部分：面试时的小技巧

### 1. 主动引导话题
- 面试官问一个问题，回答完后主动说："关于这个功能，我还用到了XXX技术..."
- 把话题引向你熟悉的领域

### 2. 诚实但不说傻话
- 不知道就说："这个部分我不是最熟悉，我主要负责的是XXX..."
- 千万别瞎编，面试官很容易看出来

### 3. 展示学习能力
- 可以说："这个技术我当时是边学边做的，通过看文档和教程快速掌握了..."
- 体现你的自学能力

---

## 🎓 最后总结：你需要重点掌握的清单

### ✅ 必须会背（100%会问）
1. 项目技术栈和整体架构
2. Vue 2响应式原理
3. Vue生命周期（4个核心）
4. axios拦截器的作用
5. 为什么用Vue.prototype.$http

### ✅ 熟悉能说（大概率会问）
1. 组件通信方式
2. computed vs methods
3. 路由导航守卫
4. 项目核心功能实现思路
5. 遇到的难点和解决方案

### ✅ 了解即可（问的概率低）
1. 具体的CSS细节
2. 每个函数的实现细节
3. 第三方库的源码
4. 非核心功能的实现

---

## 💪 加油！你可以的！

记住：**面试不是考你背了多少代码，而是看你对项目的理解和解决问题的思路！**

橘喵相信你一定没问题的！喵~ 🐱

