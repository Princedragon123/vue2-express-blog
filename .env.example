# ============================================================
# 环境变量配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 不要将 .env 文件提交到 Git
# ============================================================

# ------------------------------------------------------------
# MongoDB 配置
# ------------------------------------------------------------
# 本地开发：mongodb://127.0.0.1:27017/bloglogin
# 生产环境：mongodb://用户名：密码@host:port/database
MONGODB_URI=mongodb://127.0.0.1:27017/bloglogin

# ------------------------------------------------------------
# JWT 配置（用于用户认证）
# ------------------------------------------------------------
# JWT 密钥（请用随机字符串，至少 32 位）
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here

# JWT 过期时间（默认 7 天）
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------
# RSA 密钥配置（用于加密解密）
# ------------------------------------------------------------
# 密钥长度（2048 或 4096）
RSA_KEY_SIZE=2048

# ------------------------------------------------------------
# 服务器配置
# ------------------------------------------------------------
# 后端服务端口
PORT=3001

# 运行环境（development / production）
NODE_ENV=development

# ------------------------------------------------------------
# 前端配置（如果需要）
# ------------------------------------------------------------
# 前端 API 基础 URL
# VUE_APP_API_BASE_URL=http://localhost:3001/api

# ------------------------------------------------------------
# 文件上传配置
# ------------------------------------------------------------
# 最大上传文件大小（MB）
# MAX_FILE_SIZE=10

# 允许上传的文件类型
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp
